import 'package:flutter/material.dart';

class ListadoEventosPage extends StatelessWidget {
  const ListadoEventosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(child: Text('Eventos')),
      ),
    );
  }
}
